# Runtime dependencies for CC Translate (Windows only).
#
# install.ps1 installs these with `pip install --upgrade`; this file documents
# the same set with tested lower bounds so the app's runtime deps are declared
# in one place and can be reproduced with:
#   python -m pip install -r requirements.txt
#
# Dev-only tooling (e.g. pytest) lives in requirements-dev.txt.

pynput>=1.8.2       # global double-Ctrl+C hotkey listener
pyperclip>=1.11.0   # clipboard read/write
pystray>=0.19.5     # system tray icon + menu
Pillow>=10.0.0      # tray icon rendering / image handling (PIL)
Pygments>=2.15.0    # syntax highlighting for code results
winsdk>=1.0.0b10    # Windows.Media.Ocr (local OCR) via WinRT
comtypes>=1.2.0     # COM interop for Windows shell integration
